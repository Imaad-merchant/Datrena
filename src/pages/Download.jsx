/**
 * Public /Download route — during beta, we don't hand out the software here.
 * It just renders the Waitlist page. Once we launch, we can swap this back.
 */
import Waitlist from "./Waitlist";
export default Waitlist;
